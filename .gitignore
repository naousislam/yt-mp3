node_modules
/.svelte-kit
/build
/dist
/.vercel
/.output

# Env
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bun cache
.bun

# Debug player-script dumps that ytdl-core writes to the project root
# whenever it fails to parse YouTube's signature cipher. Harmless but
# noisy in `git status`.
*-player-script.js

# Other lockfiles (we use bun, so bun.lockb should be committed)
package-lock.json
yarn.lock
pnpm-lock.yaml
